const defaultConfig = {
  enabled: false,

  silence_threshold: 30,
  samples_threshold: 10,
  
  playback_speed: 1,
  silence_speed: 3,

  mute_silence: true,

  is_bar_collapsed: true,
};

export default defaultConfig;